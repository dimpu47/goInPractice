### Working with web services.

```
Topics covered:
- Detecting network timeouts, even when network layer doesn't formally flag them, and resuming downloads when timeouts occur.
- Parsing errors between API endpoints and client requestors by using and going beyond HTTP status headers.
- Parsing JSON, even when structure is not known ahead of time.
- Using two methods of versioning REST API and working with versioned APIs.

```
